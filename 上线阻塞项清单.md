# 上线阻塞项清单

> 版本：v1.0.0-rc.1 | 更新：2026-07-28
>
> 以下项目均为**外部依赖**，无法通过代码开发完成。全部解除后方可发布 `v1.0.0` 正式版。
> 代码和配置已准备到"只需填入真实信息即可发布"的程度。

---

## 🔴 P0 — 阻断级（不解决无法上线）

### 1. 真实微信 AppID / AppSecret
- **现状**：`client/project.config.json` 为 `touristappid`，`server/.env` 为占位值
- **操作**：微信公众平台注册小程序 → 获取 AppID/AppSecret → 替换两处
- **验证**：替换后走通真实 `wx.login` → code2session 链路
- **责任方**：产品/运营（需企业主体资质）

### 2. HTTPS + ICP 备案域名
- **现状**：无备案域名，无 HTTPS 证书
- **操作**：购买域名 → ICP 备案（约 7-20 工作日）→ 申请 HTTPS 证书 → 配置 `deploy/nginx.conf`
- **验证**：`https://域名/health` 返回 200
- **责任方**：运维

### 3. 微信后台服务器域名配置
- **现状**：未配置
- **操作**：微信公众平台 → 开发管理 → 开发设置 → 服务器域名，添加 request 合法域名（`https://api.域名`）和 downloadFile 域名（图片 CDN）
- **前置**：依赖 #2 完成
- **责任方**：产品/运营

### 4. 真实菜品图片
- **现状**：10,000 道菜使用菜系占位图（非真实照片）
- **操作**：按 `docs/IMAGE_SOURCES.md` 规范，自行拍摄/采购正版/CC0 图片，至少覆盖热门 500 道菜，上传 CDN，更新 `cover_image`
- **验证**：抽检 50 道菜图片与菜名匹配
- **责任方**：内容/设计

### 5. 生产 JWT 强密钥
- **现状**：`.env` 为开发弱密钥（生产启动会被 validateEnv 拒绝）
- **操作**：`node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"` 生成 → 写入生产 `.env`
- **验证**：生产环境正常启动（validateEnv 通过）
- **责任方**：运维

---

## 🟡 P1 — 重要（上线前应完成）

### 6. Docker 实际构建验证
- **现状**：Dockerfile/compose 已配置（非 root + HEALTHCHECK + .dockerignore），但开发机 Docker daemon 未运行，未实际 build/run
- **操作**：在有 Docker 的环境执行 `docker compose up -d --build`，验证健康检查、数据初始化、回滚
- **责任方**：运维

### 7. 微信小程序类目与资质
- **现状**：未提交审核
- **操作**：选择服务类目（餐饮-菜谱/生活服务），准备隐私政策、用户协议，提交微信审核
- **责任方**：产品/法务

### 8. 生产数据库与备份策略
- **现状**：开发用 SQLite，生产应使用 MySQL 8 + 定时备份
- **操作**：配置 MySQL，设置 mysqldump cron，验证恢复
- **责任方**：运维

---

## 🟢 P2 — 建议（上线后可迭代）

| # | 项目 | 说明 |
|---|------|------|
| 9 | 性能压测（生产环境） | MySQL + Redis 真实环境压测，当前基线为开发机 SQLite |
| 10 | CDN 加速 | 图片资源接入 CDN |
| 11 | 日志与监控 | PM2/ELK + 告警 |
| 12 | HTTPS 证书自动续期 | certbot + Let's Encrypt |
| 13 | 真实菜品视频 | 视频教程 URL 字段已预留 |

---

## 解除阻塞后的发布流程

1. 填入真实 AppID/AppSecret、JWT 密钥
2. 配置 HTTPS 域名 + Nginx
3. 微信后台配置服务器域名
4. 替换真实菜品图片
5. `docker compose up -d --build`
6. 初始化数据（migration + seed + generate-recipes）
7. 生产 Smoke Test（`/health` + 核心接口）
8. 上传小程序体验版 → 验收
9. 提交微信审核
10. 审核通过 → 发布 `v1.0.0` 正式版
