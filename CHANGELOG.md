# 变更日志 CHANGELOG

本项目所有重要变更均记录于此文件。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [v1.0.0-rc.1] - 2026-07-28

首个发布候选版本，冻结自 Git Commit `ed13d3a`。

### 新增 (Added)
- **用户偏好设置**：菜系/辣度/忌口/饮食目标/荤素/过敏原/难度/时长/厨具/人数 10 个维度，完整接入 `PUT /user/preferences`
- **个性化推荐 v2**：忌口/过敏原/荤素硬性排除，菜系/难度/时长约束，饮食目标排序，每条推荐附真实推荐理由
- **做菜模式**：大字步骤、屏幕常亮、步骤计时器、食材浮层、完成反馈
- **菜谱详情人数换算**：按用餐人数缩放食材用量（WXS 干净数字，正确处理"适量/少许"）
- **过敏原提醒**：详情页根据用户过敏原标注含过敏原食材
- **TabBar 与品牌图标**：8 个 TabBar PNG + logo/头像/空状态/分享卡片图（纯 Node 程序化生成）
- **设计系统**：`styles/tokens.wxss`（设计变量）+ `styles/base.wxss`（通用组件）
- **数据质量审计脚本**：14 类检查，输出 `quality-report.json`
- **图片策略**：13 张菜系确定性本地占位图替换全部 picsum 随机图，前端 `binderror` 兜底
- **生产环境配置校验**：弱 JWT 密钥/占位 AppID 拒绝启动
- **安全测试**：权限隔离/收藏幂等并发/SQL 注入/Redis 降级
- **性能测试脚本**：p50/p95/p99 基准 + 60s 稳定性测试
- **上线检查清单**：`docs/LAUNCH_CHECKLIST.md`

### 变更 (Changed)
- **Redis 降级策略**：`enableOfflineQueue=false` + `maxRetriesPerRequest=1`，断线时命令立即失败穿透 DB（修复前 Redis 宕机导致热门接口每请求慢 1s+，修复后 2ms）
- **限流可配置**：`RATE_LIMIT_MAX` 环境变量，生产默认 300/min/IP
- **搜索增强**：400ms 输入防抖、过期请求丢弃、无结果热门词建议
- **退出登录**：调用后端审计接口，清理本地状态
- **注册天数**：基于 `created_at` 真实计算（处理当天/无效时间/空值）
- **Dockerfile**：非 root 用户运行 + HEALTHCHECK + `.dockerignore` 排除 `.env`

### 修复 (Fixed)
- 修复 npm 依赖安装（项目路径含空格导致 node-gyp 编译 sqlite3 失败）
- 修复收藏重复点击产生重复请求
- 修复图片随机变化问题（picsum → 确定性本地占位图）

### 数据 (Data)
- 10000 道菜谱通过 14 类质量审计（无重复/缺失/注入）
- 全部封面图替换为本地菜系占位图（DB 自动备份 + 变更记录）

### 测试 (Tests)
- 62/62 集成测试通过（41 原有 + 8 推荐联动 + 13 安全）
- 语句覆盖率 83%

### 性能 (Performance)
- 热门 1.9ms / 推荐 3.8ms / 列表 3.1ms / 搜索 43.5ms / 详情 258ms（p50）
- 稳定性：60s 7130 请求 100% 成功，平均 125.7 RPS，无衰减

---

## 版本规则

- 版本冻结后，不再进行无明确用户价值的大规模重构
- 后续修改必须对应具体缺陷、测试失败、性能问题或验收问题
- 每批修复创建新 RC 版本（rc.2、rc.3...）
- 满足全部上线条件后发布 `v1.0.0` 正式版
